# Prebuilt Chroma vector index — binary/sqlite blobs, tracked via Git LFS so a normal
# `git push` doesn't hit GitHub's 100MB hard file-size limit (chroma.sqlite3 is ~101MB).
chroma_store/** filter=lfs diff=lfs merge=lfs -text
